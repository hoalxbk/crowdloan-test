"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.unwrapBytes = unwrapBytes;
exports.wrapBytes = wrapBytes;
exports.POSTFIX = exports.PREFIX = void 0;

var _util = require("@polkadot/util");

// Copyright 2019-2021 @polkadot/extension authors & contributors
// SPDX-License-Identifier: Apache-2.0
const PREFIX = (0, _util.u8aToU8a)('<Bytes>');
exports.PREFIX = PREFIX;
const POSTFIX = (0, _util.u8aToU8a)('</Bytes>');
exports.POSTFIX = POSTFIX;
const WRAP_LEN = PREFIX.length + POSTFIX.length;

function isWrapped(u8a) {
  return u8a.length >= WRAP_LEN && (0, _util.u8aEq)(u8a.subarray(0, PREFIX.length), PREFIX) && (0, _util.u8aEq)(u8a.slice(-POSTFIX.length), POSTFIX);
}

function unwrapBytes(bytes) {
  const u8a = (0, _util.u8aToU8a)(bytes);
  return isWrapped(u8a) ? u8a.subarray(PREFIX.length, u8a.length - POSTFIX.length) : u8a;
}

function wrapBytes(bytes) {
  const u8a = (0, _util.u8aToU8a)(bytes);
  return isWrapped(u8a) ? u8a : (0, _util.u8aConcat)(PREFIX, u8a, POSTFIX);
}